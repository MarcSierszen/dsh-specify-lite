# dsh-specify-lite bundle patch: register the DSH-only spec-driven development
# skill suite. Applied after dsh-base, whose @deepseek-ai/dsh-skill row provides
# the `skills` registry this plugin injects.
- insert:
    - id: specify-skills
      name: '@marcsierszen/dsh-specify-lite'
